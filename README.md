# Sub-CMD - a git-style sub command caller. 

## Install 

    npm install sub-cmd 
    
## Usage 

    var subcmd = require('sub-cmd');
    subcmd.spawn(function (err, child) {
      // ... 
    });

